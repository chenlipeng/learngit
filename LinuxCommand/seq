#seq
seq命令Shell内建命令 seq命令用于产生从某个数到另外一个数之间的所有整数。

用法:
seq [选项]... 尾数
seq [选项]... 首数 尾数
seq [选项]... 首数 增量 尾数

-f
--format=格式
使用printf 样式的浮点格式,不能和-w一起用

-s
--separator=字符串
使用指定字符串分隔数字（默认使用：\n）

-w
--equal-width
在列前添加0 使得宽度相同,不能和-f一起用

例如:
	sed -f "%03g" 9 11
	这样的话数字位数不足部分是0;
	% 前面制定字符串;
	% 后面指定数字的位数 默认是"%g";
